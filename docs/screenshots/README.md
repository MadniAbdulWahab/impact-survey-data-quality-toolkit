# Screenshot provenance

These images were captured on 11 July 2026 from tested local outputs containing
synthetic data only. Each image comes directly from a tested project artifact.

- `excel_pivot_region_round_correct.png`: captured from the actual
  `Pivot_Region_Round` sheet in the verified macro workbook; grand total 420.
- `excel_pivot_region_chart.png`: captured in Excel from a PivotChart based on
  the same region-by-round pivot results.
- `excel_qc_export.png`: exported from the actual `QC_Export` sheet after the
  embedded VBA macro ran.
- `excel_monitoring_summary.png`: exported from the template's compact
  `MonitoringSummary` sheet.
- `quarto_report_overview.png`: headless-browser screenshot of the rendered
  `reports/impact_survey_report.html` file.
